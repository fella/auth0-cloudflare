{
  "name": "auth0-worker",
  "type": "module",
  "dependencies": {
    "jose": "^5.3.0"
  },
  "scripts": {
    "build": "cd cloudflare-app && npm install && npm run build",
    "deploy": "wrangler deploy",
    "deploy:worker": "cd cloudflare-app && wrangler deploy"
  }
}